# Production environment — Frontend is served by the same Express backend,
# so API calls use a RELATIVE path (/api) — DO NOT put localhost here!
# Leave VITE_API_URL empty so api.js falls back to '/api'
VITE_API_URL=https://klarehomes.com
VITE_WHATSAPP_NUMBER=
